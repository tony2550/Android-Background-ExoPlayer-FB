package com.com.clone_spotify.util;

import com.com.clone_spotify.repository.dto.Song;
import com.google.gson.Gson;

import java.lang.reflect.Type;
import java.util.List;

import retrofit2.Response;

public class MyConvert {
//
//    public static List<Song> toPostCollect(Response<Song> response){
//        Gson gson = new Gson();
//        Type collectionType = new TypeToken<List<Song>>(){}.getType();
//        List<Song> result = gson.fromJson(gson.toJson(response.body().getData()), collectionType);
//        return result;
//    }
}
